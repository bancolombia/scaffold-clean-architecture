package {{package}}.redis.repository

import {{package}}.redis.repository.helper.ReactiveRepositoryAdapterOperations
import org.reactivecommons.utils.ObjectMapper
import org.springframework.stereotype.Repository


@Repository
open class ReactiveRedisRepositoryAdapter(repository: ReactiveRedisRepository, mapper: ObjectMapper) :
    ReactiveRepositoryAdapterOperations<Any, Any, String, ReactiveRedisRepository>(
        repository,
        mapper,
        { d ->
            mapper.map(
                d,
                Any::class.java /* change for domain model */
            )
        }) // implements ModelRepository from domain
