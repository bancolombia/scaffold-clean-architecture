package {{package}}.mq.sender;

import {{package}}.mq.common.api.MQMessageSender;
{{#lombok}}
import lombok.AllArgsConstructor;
{{/lombok}}
import org.springframework.stereotype.Component;
import reactor.core.publisher.Mono;

@Component
{{#lombok}}
@AllArgsConstructor
{{/lombok}}
public class SampleMQMessageSender /* implements SomeGateway */ {
    private final MQMessageSender sender;
    {{^lombok}}

    public MyMessageSender(MQMessageSender sender) {
        this.sender = sender;
    }
    {{/lombok}}

    public Mono<String> send(String message) {
        return sender.send(context -> context.createTextMessage(message));
    }
}
