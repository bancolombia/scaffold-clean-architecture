---
sidebar_position: 7
---

# Generate Helper

The **`generateHelper | gh`** task will generate a module in Infrastructure layer, this task has one required
parameter `name`.

```shell
   gradle generateHelper --name=[helperName]
   gradle gh --name=[helperName]
   ```