server:
  port: 8080

spring:
  application:
    name: "Default"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
  data:
    mongodb:
      authentication-database: admin
      username: root
      password: example
      database: orders
      port: 27017
      host: localhost